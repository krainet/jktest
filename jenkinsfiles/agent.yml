apiVersion: v1
kind: Pod
metadata:
  annotations:
spec:
  containers:
  - name: jnlp
    image: jenkinsci/jnlp
    resources:
     limits:
      memory: 128Mi
     requests:
      cpu: "0.1"
      memory: 128Mi
    securityContext:
      privileged: true

