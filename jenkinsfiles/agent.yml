apiVersion: v1
kind: Pod
metadata:
  annotations:
spec:
  containers:
  - name: jnlp
    image: jenkins/jnlp-slave:3.10-1
    resources:
     limits:
      memory: 512Mi
     requests:
      cpu: ""
      memory: 512Mi
    securityContext:
      privileged: true

